[project]
name = "py-binding-test"
version = "0.1.0"
description = "Sandbox to test the python binding"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "compas>=2.15.1",
    "compas-viewer>=2.0.2",
    "scipy==1.16.1"
]
